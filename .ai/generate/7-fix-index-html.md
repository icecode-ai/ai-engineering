## `index.html` 调整

### `流程` 模块调整

tab 切换四种流程，具体交互和样式风格，你帮我设计，最好让用户通俗易懂

* `自动化端到端流程` (调用 `ai-goal-e2e-auto`)
* `探索式端到端流程` (调用 `ai-goal-e2e-explore`)
* `自动化流程` (调用 `ai-goal-auto`)
* `手动流程` （也就是现在的）


### `命令` 模块调整

1. `目标指令` 改成 `工作流指令`，放在 `Spec 指令` 上方
2. `/ai-goal-auto` 卡片和 `/ai-goal-e2e-auto` 卡片位置交换下
3. `/ai-goal-auto` 铺满全宽，不要有留白