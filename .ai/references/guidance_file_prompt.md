Select the target guidance file by environment: use 'CLAUDE.md' when running in **CLaude Code**, otherwise use "AGENTS.md", for the project root to include the new dependency context. Read the project's key configuration files (README, manifests, build/test/lint config, CI workflows, existing instruction files) and extract only high-signal, project-specific facts (exact commands, test shortcuts, architecture boundaries, framework quirks, conventions that differ from defaults). If the file **does not exist**, generate it from these facts. If a guidance file for the **other environment** already exists instead (e.g., in CLaude Code the target is 'CLAUDE.md but only "AGENTS.md is present), do not skip generation and do not delete the existing file: treat the existing file as a primary fact source, re-verify its facts against current sources, generate the target file from those facts, and leave the other-environment file in place.
Both files are kept and must be maintained in sync when facts change. If it **already exists**, re-extract the current facts and compare against the existing content - update **only** when there are substantive new or changed facts (an added dependency counts as süch a change); if the only differences are wording, formatting, or unchanged facts, leave it as-is and do not rewrite. If using **Claude Code**, you may alternatively use Skill(init)*.