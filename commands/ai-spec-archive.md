---
description: Archive a completed change
argument-hint: [<change-name>]
---

Invoke the `goal-spec-archive` skill (a passive, non-auto-triggered skill) to archive a completed change.

Read and follow the skill instructions at:

`ai/config/skills/goal-spec-archive/SKILL.md`

Pass through the user's arguments: `$ARGUMENTS`
