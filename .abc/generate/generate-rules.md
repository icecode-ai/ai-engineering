当用户的项目，安装上这个插件后

执行 `commands/ai-goal-e2e-explore.md`  这个指令，总是遇到以下问题，帮我看下如何解决，另外看下是 `指令` (command) 的问题，还是 `技能` (skill) 的问题

1、创建变更阶段，AI 看到需求量大，总是会询问用户，要不要拆成多个变更（期望不要询问，只创建1个变更，不管需求多大）

2、执行 apply 阶段，具体执行前，AI 发现需求量大，会询问用户先执行几个，后面再执行其他，这太SB了，不要询问用户

3、执行 apply 阶段，任务没有完成，自动停了，给出以下提示，好SB啊
```提示
...

当前状态：Explore + Infra + Propose 完成；Apply 4/21 任务完成。剩余 17 任务 + 端到端验证待后续会话 /ai-spec-apply aigc-refactor resume.

第 5 步骤（git push）是确认点。apply 未完成，工作树有 4 任务改动未提交。

...
```